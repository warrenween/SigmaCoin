package org.opensecreto.sigmascript;

public class BytecodeExecutor {

    public static int execute(StorageManager storage) {
        byte[] stack = new byte[Config.MAX_STACK];
        int stackPos = 0;
        byte[] heap = new byte[Config.MAX_HEAP];

        return 0;
    }

}
