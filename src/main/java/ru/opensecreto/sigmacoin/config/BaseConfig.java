package ru.opensecreto.sigmacoin.config;

/**
 * Default protocol constants. These must not be changed.
 */
public class BaseConfig {

    /**
     * Address, block and transaction hash size in bytes.
     * Chose to be equal to SVM word size.
     */
    public static final int DEFAULT_IDENTIFICATOR_LENGTH = 32;
}
