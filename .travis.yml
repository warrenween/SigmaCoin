language: java
before_install:
  - chmod +x gradlew
install:
#  - curl -sL https://github.com/jpm4j/jpm4j.installers/raw/master/dist/biz.aQute.jpm.run.jar >jpm4j.jar
#  - java -jar jpm4j.jar -u init
#  - npm install jpm
#  - curl -sL https://github.com/jpm4j/jpm4j.installers/raw/master/dist/biz.aQute.jpm.run.jar >t.jar
#  - java -jar t.jar init
#  - rm t.jar
#  - sudo jpm install com.codacy:codacy-coverage-reporter:assembly
after_script:
#  - codacy-coverage-reporter -l Java -r build\reports\jacoco\jacocoTestReport.xml
#  - mvn com.gavinmogan:codacy-maven-plugin:coverage -DcoverageReportFile=build\reports\jacoco\jacocoTestReport.xml -DprojectToken=%CODACY_PROJECT_TOKEN%
  - ./gradlew sendCoverageToCodacy
jdk:
  - oraclejdk8
  #- openjdk8
